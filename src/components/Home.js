import React from 'react'
import Typography from '@material-ui/core/Typography'
import { withStyles } from '@material-ui/core/styles'

const styles = () => ({})

const Home = () => (
  <div>
    <Typography>Top画面だよ</Typography>
  </div>
)

export default withStyles(styles)(Home)
